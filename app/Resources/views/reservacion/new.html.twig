{% extends 'base.html.twig' %}

{% block body %}
    <h1>Creando Reservación</h1>
    <hr/>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-success" />
    {{ form_end(form) }}
    <hr/>
    <a href="{{ path('reservacion_index') }}" class="btn btn-primary" >Regresar</a>           
{% endblock %}
